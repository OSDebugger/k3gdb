import{_ as a,o as n,c as p,a2 as e}from"./chunks/framework.Ca9F9Y59.js";const b=JSON.parse('{"title":"K3 严格冷启动直接进入 StarryOS","description":"","frontmatter":{},"headers":[],"relativePath":"2026-09-21-K3冷启动StarryOS.md","filePath":"2026-09-21-K3冷启动StarryOS.md"}'),t={name:"2026-09-21-K3冷启动StarryOS.md"};function i(l,s,o,c,d,h){return n(),p("div",null,[...s[0]||(s[0]=[e(`<h1 id="k3-严格冷启动直接进入-starryos" tabindex="-1">K3 严格冷启动直接进入 StarryOS <a class="header-anchor" href="#k3-严格冷启动直接进入-starryos" aria-label="Permalink to &quot;K3 严格冷启动直接进入 StarryOS&quot;">​</a></h1><p>之前我们的稳定流程一直是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>K3 上电</span></span>
<span class="line"><span>→ 先启动 Bianbu Linux</span></span>
<span class="line"><span>→ 完成 RCPU / RPMI 等平台初始化</span></span>
<span class="line"><span>→ warm reboot</span></span>
<span class="line"><span>→ FORCE_RECOVERY</span></span>
<span class="line"><span>→ Debug FSBL</span></span>
<span class="line"><span>→ OpenSBI</span></span>
<span class="line"><span>→ U-Boot</span></span>
<span class="line"><span>→ StarryOS</span></span></code></pre></div><p>也就是说，之前一直认为 <strong>必须先启动一次官方 Bianbu Linux</strong>，让板子内部的一些管理模块进入正确状态，然后再 warm reboot，Debug 启动链才能稳定工作。</p><p>这个流程虽然能用，但比较麻烦，而且老师这次也明确提出，希望解决两个问题：</p><ol><li>K3 能不能真正从冷启动直接进入 StarryOS；</li><li>后面能不能把现在这一大串人工操作自动化。</li></ol><p>所以这一阶段首先重新检查了第一个问题。</p><h2 id="_1-之前为什么怀疑冷启动有问题" tabindex="-1">1. 之前为什么怀疑冷启动有问题 <a class="header-anchor" href="#_1-之前为什么怀疑冷启动有问题" aria-label="Permalink to &quot;1. 之前为什么怀疑冷启动有问题&quot;">​</a></h2><p>历史实验里曾经出现过一次比较典型的失败：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BootROM</span></span>
<span class="line"><span>→ Debug FSBL</span></span>
<span class="line"><span>→ OpenSBI</span></span>
<span class="line"><span>→ 没有进入 Full U-Boot</span></span></code></pre></div><p>当时通过 JTAG 抓到 CPU 位于 OpenSBI 的：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>csi_dcache_invalid_range()</span></span>
<span class="line"><span>__smq_rx()</span></span></code></pre></div><p>后来继续分析发现，这里实际上属于 <strong>RPMI shared-memory mailbox</strong> 的接收流程。</p><p>RPMI 可以简单理解成：</p><blockquote><p>AP 主 CPU 和 RCPU 管理核之间用来交换电源、时钟、HSM 等平台管理消息的一套通信协议。</p></blockquote><p>OpenSBI 会通过共享内存向 RCPU 发请求，然后等待 RCPU 返回 ACK。</p><p>进一步检查历史现场后发现，当时 P2A_ACK 接收队列的：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>head = 0x10000000</span></span>
<span class="line"><span>tail = 0x00040300</span></span></code></pre></div><p>都是非法值。</p><p>而正常队列只有 14 个 slot，因此合法范围应该是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>0 ~ 13</span></span></code></pre></div><p>这说明历史失败现场的 RPMI shared memory 确实处于异常状态。</p><h2 id="_2-把-opensbi-的-rpmi-初始化流程查清楚" tabindex="-1">2. 把 OpenSBI 的 RPMI 初始化流程查清楚 <a class="header-anchor" href="#_2-把-opensbi-的-rpmi-初始化流程查清楚" aria-label="Permalink to &quot;2. 把 OpenSBI 的 RPMI 初始化流程查清楚&quot;">​</a></h2><p>这一轮又继续把 OpenSBI 源码里的调用关系完整追了一遍。</p><p>目前已经确认，K3 OpenSBI 启动时最早的一条 RPMI 请求是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Base Service Group</span></span>
<span class="line"><span>Service ID = 0x06</span></span>
<span class="line"><span>Probe Service Group</span></span></code></pre></div><p>通过：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>A2P_REQ</span></span></code></pre></div><p>发送，再从：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>P2A_ACK</span></span></code></pre></div><p>等待响应。</p><p>默认接收超时是 <strong>20 ms</strong>。</p><p>一开始我们以为，如果 RCPU 没有响应，这里可能会直接把 OpenSBI 卡死。</p><p>但源码继续往上追以后发现并不是这样。</p><p>实际错误传播大概是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>RPMI 请求超时</span></span>
<span class="line"><span>→ rpmi_shmem_mbox_init() 返回 timeout</span></span>
<span class="line"><span>→ RPMI HSM 初始化失败</span></span>
<span class="line"><span>→ fdt_hsm_init() 忽略这个错误</span></span>
<span class="line"><span>→ OpenSBI 继续启动</span></span>
<span class="line"><span>→ Full U-Boot</span></span></code></pre></div><p>也就是说，RPMI/HSM 在这里属于一种<strong>可选的平台管理能力</strong>，初始化失败并不会阻止当前启动 hart 继续进入 U-Boot。</p><p>这也解释了一个之前一直觉得矛盾的问题：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Debug FIT 明明没有 ESOS / RCPU firmware</span></span>
<span class="line"><span>为什么冷启动还是有可能进入 Full U-Boot？</span></span></code></pre></div><p>答案是：</p><blockquote><p>进入 Full U-Boot 本身并不能证明 RPMI 或 RCPU 正常。</p></blockquote><p>OpenSBI 可以在 RPMI 初始化失败后继续运行。</p><h2 id="_3-debug-golden-flow-并不会偷偷加载-esos" tabindex="-1">3. Debug Golden Flow 并不会偷偷加载 ESOS <a class="header-anchor" href="#_3-debug-golden-flow-并不会偷偷加载-esos" aria-label="Permalink to &quot;3. Debug Golden Flow 并不会偷偷加载 ESOS&quot;">​</a></h2><p>为了确认是不是 Debug FSBL 又从 QSPI 的 <code>esos</code> 分区偷偷加载了 RCPU firmware，这次也专门把 SPL 的加载流程查了一遍。</p><p>结果比较明确：</p><p>USB Recovery 模式下：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BootROM</span></span>
<span class="line"><span>→ FSBL</span></span>
<span class="line"><span>→ BOOT_DEVICE_BOARD</span></span>
<span class="line"><span>→ fastboot 下载 combined FIT</span></span></code></pre></div><p>而 <code>board_load_extra_fits()</code> 没有 <code>BOOT_DEVICE_BOARD</code> 对应的 ESOS 加载分支。</p><p>同时冻结的：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>k3-jtag-ram-opensbi-uboot.itb</span></span></code></pre></div><p>里面只有：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>OpenSBI</span></span>
<span class="line"><span>U-Boot</span></span>
<span class="line"><span>DTB</span></span></code></pre></div><p>没有：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rcpu0-fw</span></span>
<span class="line"><span>rcpu1-fw</span></span>
<span class="line"><span>ESOS</span></span></code></pre></div><p>所以当前 Debug Golden Flow 本身不会重新执行 <code>rproc_load()</code> / <code>rproc_start()</code> 去启动 RCPU。</p><p>这一步也让整个启动链的理解更清楚了：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>能启动 U-Boot</span></span>
<span class="line"><span>≠</span></span>
<span class="line"><span>RPMI 正常</span></span>
<span class="line"><span>≠</span></span>
<span class="line"><span>RCPU / ESOS 正常</span></span></code></pre></div><p>以前这几个概念有点混在一起，现在已经拆开了。</p><h2 id="_4-重新进行了真正的-strict-cold-实验" tabindex="-1">4. 重新进行了真正的 Strict Cold 实验 <a class="header-anchor" href="#_4-重新进行了真正的-strict-cold-实验" aria-label="Permalink to &quot;4. 重新进行了真正的 Strict Cold 实验&quot;">​</a></h2><p>为了排除之前 warm boot 状态残留的影响，这次重新设计了比较严格的冷启动实验。</p><p>断电时同时断开：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>K3 主电源</span></span>
<span class="line"><span>USB Download</span></span>
<span class="line"><span>J-Link</span></span>
<span class="line"><span>CH340 USB</span></span></code></pre></div><p>CH340 与板子只连接：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TX</span></span>
<span class="line"><span>RX</span></span>
<span class="line"><span>GND</span></span></code></pre></div><p>没有连接 3.3V / 5V / VCC。</p><p>完全断电等待以后，再从：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>FORCE_RECOVERY</span></span>
<span class="line"><span>→ BootROM USB Recovery</span></span></code></pre></div><p>重新开始。</p><p>BootROM 状态确认：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>version-brom: 1.0</span></span></code></pre></div><p>随后重新上传 Debug FSBL：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BootROM</span></span>
<span class="line"><span>→ FSBL</span></span></code></pre></div><p>SPL 正常出现：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>U-Boot SPL 2022.10-dirty</span></span>
<span class="line"><span>DDR training consume ...</span></span>
<span class="line"><span>Debug JTAG enabled on MMC1 pins</span></span></code></pre></div><p>然后继续上传：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>k3-jtag-ram-opensbi-uboot.itb</span></span></code></pre></div><p>最终成功出现：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>U-Boot 2022.10-dirty</span></span>
<span class="line"><span>CPU: rv64imafdcvh</span></span>
<span class="line"><span>Model: spacemit k3 com260 ifx board</span></span>
<span class="line"><span>DRAM: 8 GiB</span></span></code></pre></div><p>也就是说：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>严格冷启动</span></span>
<span class="line"><span>→ Debug FSBL</span></span>
<span class="line"><span>→ OpenSBI</span></span>
<span class="line"><span>→ Full U-Boot</span></span></code></pre></div><p>已经能够直接成功。</p><p>而且这个实验后面又重新重复了一次，在完全不接 JTAG、不使用 GDB 干扰启动的情况下，结果仍然成功。</p><p>因此以前的：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>必须先启动 Bianbu</span></span>
<span class="line"><span>→ warm reboot</span></span>
<span class="line"><span>→ 才能 Debug 启动</span></span></code></pre></div><p>已经不能继续作为固定前置条件。</p><p>目前更准确的结论应该是：</p><blockquote><p>历史上确实发生过 cold direct-Recovery 下的异常，但它不是一个稳定必现条件。</p></blockquote><h2 id="_5-strict-cold-下-ufs-也成功了" tabindex="-1">5. Strict Cold 下 UFS 也成功了 <a class="header-anchor" href="#_5-strict-cold-下-ufs-也成功了" aria-label="Permalink to &quot;5. Strict Cold 下 UFS 也成功了&quot;">​</a></h2><p>进入 Full U-Boot 以后，继续执行：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>scsi scan</span></span></code></pre></div><p>这一步是让 U-Boot 初始化 K3 的 UFS 控制器，并扫描实际的存储设备。</p><p>结果成功识别：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Vendor: KINGSTON</span></span>
<span class="line"><span>Prod.: TY7B-128</span></span>
<span class="line"><span>Capacity: 119.2 GB</span></span></code></pre></div><p>说明在这次真正的冷启动条件下：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>K3 UFS</span></span>
<span class="line"><span>→ MPHY</span></span>
<span class="line"><span>→ Host Controller</span></span>
<span class="line"><span>→ Device Scan</span></span></code></pre></div><p>也是能够正常工作的。</p><p>这一步很关键，因为 StarryOS 的 rootfs 最终还是需要从这块 UFS 上读取。</p><h2 id="_6-冷启动直接进入-starryos" tabindex="-1">6. 冷启动直接进入 StarryOS <a class="header-anchor" href="#_6-冷启动直接进入-starryos" aria-label="Permalink to &quot;6. 冷启动直接进入 StarryOS&quot;">​</a></h2><p>随后按照已经冻结的 Host-DWARF 启动方式，把 StarryOS kernel 下载到：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>0x140000000</span></span></code></pre></div><p>对应文件：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>starryos_host_dwarf_release.bin</span></span></code></pre></div><p>然后把 DTB 下载到：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>0x138000000</span></span></code></pre></div><p>对应文件：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>spacemit-k3-com260-ifx.dtb</span></span></code></pre></div><p>最后执行：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>booti 0x140000000 - 0x138000000</span></span></code></pre></div><p>这一套仍然是我们之前已经验证过的 StarryOS 人工启动方式。</p><p>最终串口成功出现：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Welcome to Starry OS!</span></span></code></pre></div><p>因此这一次已经实际完成：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>完全断电</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>BootROM Recovery</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>Debug FSBL / SPL</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>OpenSBI</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>Full U-Boot</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>UFS / KINGSTON</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>Host-DWARF StarryOS</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>Welcome to Starry OS!</span></span></code></pre></div><p>而整个过程：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>没有先启动 Bianbu</span></span>
<span class="line"><span>没有 warm reboot</span></span>
<span class="line"><span>没有依赖 Linux 做前置初始化</span></span></code></pre></div><h2 id="_7-这次结果意味着什么" tabindex="-1">7. 这次结果意味着什么 <a class="header-anchor" href="#_7-这次结果意味着什么" aria-label="Permalink to &quot;7. 这次结果意味着什么&quot;">​</a></h2><p>这一阶段最大的变化，其实不是又多跑通了一次 StarryOS，而是我们把之前对冷启动问题的理解修正了。</p><p>以前的理解比较像：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cold boot</span></span>
<span class="line"><span>→ RCPU/RPMI 没准备好</span></span>
<span class="line"><span>→ OpenSBI 卡死</span></span>
<span class="line"><span>→ 必须 Bianbu warm boot</span></span></code></pre></div><p>现在应该改成：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>历史 cold 异常</span></span>
<span class="line"><span>→ 曾经观察到 RPMI queue 状态异常</span></span>
<span class="line"><span>→ OpenSBI 当时正在 RPMI RX 重试</span></span>
<span class="line"><span>→ 但 RPMI timeout 本身不会阻止 OpenSBI继续</span></span>
<span class="line"><span>→ 历史失败还存在其他条件性因素</span></span></code></pre></div><p>与此同时：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Strict cold</span></span>
<span class="line"><span>→ Debug Golden Flow</span></span>
<span class="line"><span>→ Full U-Boot</span></span>
<span class="line"><span>→ UFS</span></span>
<span class="line"><span>→ StarryOS</span></span></code></pre></div><p>已经被真板实际验证。</p><p>所以从当前工程目标来看：</p><blockquote><p><strong>“先启动 Bianbu 再 warm reboot”已经可以从 StarryOS Debug Golden Flow 的必需步骤中删除。</strong></p></blockquote><p>至于 RCPU / RPMI 在 strict cold 下到底有没有完整工作，目前仍然没有完全证明。</p><p>这件事和“StarryOS能不能直接冷启动”现在已经可以拆成两个问题：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>问题 A：</span></span>
<span class="line"><span>Cold → StarryOS</span></span>
<span class="line"><span>目前已经验证成功。</span></span>
<span class="line"><span></span></span>
<span class="line"><span>问题 B：</span></span>
<span class="line"><span>Cold → RCPU / ESOS / RPMI 完整平台管理能力</span></span>
<span class="line"><span>仍需要后续单独验证。</span></span></code></pre></div><p>这样就不会再因为 RPMI问题，把整个 StarryOS启动链一起判定为失败。</p><h2 id="_8-下一阶段" tabindex="-1">8. 下一阶段 <a class="header-anchor" href="#_8-下一阶段" aria-label="Permalink to &quot;8. 下一阶段&quot;">​</a></h2><p>老师提出的第一个目标：</p><blockquote><p>K3 真正冷启动直接进入 StarryOS。</p></blockquote><p>目前已经基本完成。</p><p>接下来可以开始第二个目标：</p><blockquote><p><strong>把现在繁琐的人工 Golden Flow 自动化。</strong></p></blockquote><p>现在人工流程已经非常明确：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BootROM Recovery</span></span>
<span class="line"><span>→ 上传 FSBL</span></span>
<span class="line"><span>→ 等待 SPL</span></span>
<span class="line"><span>→ 上传 OpenSBI + U-Boot FIT</span></span>
<span class="line"><span>→ 等待 Full U-Boot</span></span>
<span class="line"><span>→ scsi scan</span></span>
<span class="line"><span>→ 上传 StarryOS kernel</span></span>
<span class="line"><span>→ 上传 DTB</span></span>
<span class="line"><span>→ booti</span></span>
<span class="line"><span>→ 等待 StarryOS ready</span></span></code></pre></div><p>现有脚本虽然已经能封装部分 Host 侧 fastboot 命令，但很多步骤仍然需要人工：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>看串口</span></span>
<span class="line"><span>按 Enter</span></span>
<span class="line"><span>输入 scsi scan</span></span>
<span class="line"><span>启动 U-Boot fastboot</span></span>
<span class="line"><span>输入 booti</span></span>
<span class="line"><span>人工判断是否成功</span></span></code></pre></div><p>所以接下来计划把它改成一个真正的<strong>状态驱动启动流程</strong>：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>检测 BootROM</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>自动识别 SPL ready</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>自动识别 Full U-Boot ready</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>自动执行 UFS 初始化</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>自动加载 kernel / DTB</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>自动执行 booti</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>自动等待 Welcome to Starry OS!</span></span></code></pre></div><p>这里的核心是让脚本根据<strong>板子的真实串口状态</strong>决定下一步。</p><p>这样以后 K3 + StarryOS + ARD 的日常调试，就可以从现在这种比较长的人工操作，逐步变成真正稳定的一键启动流程。</p>`,144)])])}const u=a(t,[["render",i]]);export{b as __pageData,u as default};
