import{_ as a,o as n,c as p,a2 as e}from"./chunks/framework.Ca9F9Y59.js";const g=JSON.parse('{"title":"ARD + K3 调试环境适配总结","description":"","frontmatter":{},"headers":[],"relativePath":"2026-09-02-ARD-K3调试环境适配总结.md","filePath":"2026-09-02-ARD-K3调试环境适配总结.md"}'),t={name:"2026-09-02-ARD-K3调试环境适配总结.md"};function i(l,s,c,o,d,r){return n(),p("div",null,[...s[0]||(s[0]=[e(`<h1 id="ard-k3-调试环境适配总结" tabindex="-1">ARD + K3 调试环境适配总结 <a class="header-anchor" href="#ard-k3-调试环境适配总结" aria-label="Permalink to &quot;ARD + K3 调试环境适配总结&quot;">​</a></h1><p>本阶段是围绕“让 K3 硬件成为可进行 Rust 源码级调试的目标平台”完成了一次完整的调试环境适配。</p><p>首先，通过实验梳理并固定了 K3 的调试启动链：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BootROM</span></span>
<span class="line"><span>→ Debug FSBL / SPL</span></span>
<span class="line"><span>→ OpenSBI</span></span>
<span class="line"><span>→ Full U-Boot</span></span>
<span class="line"><span>→ UFS 初始化</span></span>
<span class="line"><span>→ StarryOS</span></span>
<span class="line"><span>→ OpenOCD</span></span>
<span class="line"><span>→ J-Link / JTAG</span></span>
<span class="line"><span>→ GDB</span></span></code></pre></div><p>在此基础上，对启动环境进行了必要改造。</p><hr><h2 id="让-starryos-跑起来" tabindex="-1">让 StarryOS 跑起来 <a class="header-anchor" href="#让-starryos-跑起来" aria-label="Permalink to &quot;让 StarryOS 跑起来&quot;">​</a></h2><p>想要实现最终目标，最开始先解决的是：<strong>StarryOS 在 K3 这块板子上跑起来</strong></p><p>K3 的启动过程：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>芯片上电</span></span>
<span class="line"><span>→ 第一阶段启动程序</span></span>
<span class="line"><span>→ OpenSBI</span></span>
<span class="line"><span>→ U-Boot</span></span>
<span class="line"><span>→ StarryOS</span></span>
<span class="line"><span>→ UFS 根文件系统</span></span></code></pre></div><p>在部署期间发现，StarryOS 想正常访问 UFS，需要前面的 U-Boot 先帮忙把 UFS 初始化。</p><p>于是在解决这个问题之后形成了一套验证成功的启动流程：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>进入恢复模式</span></span>
<span class="line"><span>→ 临时加载自己的启动程序</span></span>
<span class="line"><span>→ 进入 U-Boot</span></span>
<span class="line"><span>→ 初始化 UFS</span></span>
<span class="line"><span>→ 加载 StarryOS</span></span>
<span class="line"><span>→ 启动</span></span>
<span class="line"><span>→ 进入 StarryOS shell</span></span></code></pre></div><p>算是第一次真正完成了：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>K3 + StarryOS 真板运行 ✅</span></span></code></pre></div><p>后来为了方便输入指令，还把这套流程做成了一个自动输入指令的启动脚本。</p><hr><h2 id="解决-jtag-与-控制-cpu" tabindex="-1">解决 JTAG 与 控制 CPU <a class="header-anchor" href="#解决-jtag-与-控制-cpu" aria-label="Permalink to &quot;解决 JTAG 与 控制 CPU&quot;">​</a></h2><p>StarryOS 能跑以后就可以正式思考<strong>如何让电脑直接控制 K3 CPU</strong></p><p>根据我们调试的链路来看似乎是成立的：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>电脑</span></span>
<span class="line"><span>→ J-Link</span></span>
<span class="line"><span>→ JTAG</span></span>
<span class="line"><span>→ K3 CPU</span></span></code></pre></div><p>但是一开始一直遇到：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>JTAG all zeroes</span></span></code></pre></div><p>简单说，就是电脑正常，J-Link 也正常，但 K3 根本没有把真正的 JTAG 信号接出来。</p><p>后来我通过官方资料用户手册确认 <strong>K3 的 JTAG 和 TF/MMC1 引脚是复用的。</strong></p><p>于是我修改了启动程序，让它在启动时完成把 MMC1 引脚切换成 JTAG 的设置，不出所料，串口出现了：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Debug JTAG enabled on MMC1 pins</span></span></code></pre></div><p>从这以后，J-Link 终于真正识别到了 K3，我们则是第一次打通了JTAG的通路：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>OpenOCD</span></span>
<span class="line"><span>→ J-Link</span></span>
<span class="line"><span>→ K3</span></span></code></pre></div><p>JTAG 接通以后，我开始让电脑直接控制 K3 CPU进行一系列的基础控制，完成了：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>识别 CPU ✅</span></span>
<span class="line"><span>暂停 CPU ✅</span></span>
<span class="line"><span>读取寄存器 ✅</span></span>
<span class="line"><span>查看当前运行位置 ✅</span></span>
<span class="line"><span>继续运行 ✅</span></span></code></pre></div><p>的基础调试。这一步又可以证明了一件事：<strong>已经能从外部停住 K3 CPU。</strong></p><p>到这里，硬件调试链就算正式成立。</p><hr><h2 id="接入gdb并命中函数断点" tabindex="-1">接入GDB并命中函数断点 <a class="header-anchor" href="#接入gdb并命中函数断点" aria-label="Permalink to &quot;接入GDB并命中函数断点&quot;">​</a></h2><p>硬件调试链通了，那接下来需要接入 GDB。但是 GDB 自己其实并不会直接控制 K3 芯片的 JTAG 引脚，我们需要一个连接 GDB 和真板硬件之间的“调试中间层”，它就是 OpenOCD 。</p><p>OpenOCD在链路中的位置是在这里：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GDB</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>OpenOCD</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>J-Link</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>K3</span></span>
<span class="line"><span>↓</span></span>
<span class="line"><span>正在运行的 StarryOS</span></span></code></pre></div><p>在这一阶段，我们验证了：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GDB 连接 K3   ✅</span></span>
<span class="line"><span>读取寄存器     ✅</span></span>
<span class="line"><span>查看程序地址   ✅</span></span>
<span class="line"><span>暂停和继续运行 ✅</span></span></code></pre></div><p>随后又往前走了一步：</p><p><strong>让 GDB 根据当前地址，找到 StarryOS 里的对应函数。</strong></p><p>我们把板子上正在运行的 StarryOS，配上同一次编译得到的 ELF 文件，成功看到了：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>当前运行地址</span></span>
<span class="line"><span>→ 对应到 StarryOS 中的某个函数</span></span></code></pre></div><p>这说明板子上的程序和电脑上的符号文件已经成功对应起来了。</p><p>然后做了一个很关键的实验：直接在 StarryOS 的某个函数上设置断点。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>设置断点</span></span>
<span class="line"><span>→ 继续运行</span></span>
<span class="line"><span>→ CPU 停在这个函数</span></span></code></pre></div><p>经过反复命中的验证：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>continue</span></span>
<span class="line"><span>→ 再次命中</span></span>
<span class="line"><span>→ continue</span></span>
<span class="line"><span>→ 再次命中</span></span></code></pre></div><p>可以说，已经完成了基本打通 GDB 到 K3 之间的调试链路的任务：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>K3 硬件调试       ✅</span></span>
<span class="line"><span>OpenOCD          ✅</span></span>
<span class="line"><span>GDB              ✅</span></span>
<span class="line"><span>StarryOS 函数断点 ✅</span></span></code></pre></div><p>GDB 已经能看到函数名了。但是，为了接入我们的ARD函数调试器，我们需要看到：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>具体源码文件</span></span>
<span class="line"><span>第几行代码</span></span>
<span class="line"><span>变量信息</span></span></code></pre></div><p>但是原来运行的 StarryOS 虽然带有函数符号，却没有完整的源码调试信息。</p><p>所以当时可以做到：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>break memcpy       ✅</span></span></code></pre></div><p>这是没问题的，但想满足我们调试器的需求，真正需要的是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>break k3_ufs.rs:1799</span></span></code></pre></div><p>也就是 <strong>直接按源码文件和行号调试。</strong></p><hr><h2 id="重新编译验证带debug编译信息的starryos" tabindex="-1">重新编译验证带debug编译信息的StarryOS <a class="header-anchor" href="#重新编译验证带debug编译信息的starryos" aria-label="Permalink to &quot;重新编译验证带debug编译信息的StarryOS&quot;">​</a></h2><p>一开始重新编译之后确实尝试过普通的 <strong>--debug</strong> ，但它有一个弊端，就是会把整个 StarryOS 从 release 版本，变成“开发版本”，很多编译行为都会跟着发生变化。但想满足调试器控制硬件需求，最理想的情况是：<strong>板子上跑的程序仍然保持原来的 release 逻辑，只让电脑上的 ELF 多带一些调试信息。</strong></p><p>于是找到了一个更加合适的方案：Host-DWARF，就是把源码调试信息留在电脑上的 ELF 文件里，不让这些信息去膨胀板子实际运行的 kernel。这个方案成功得到了：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>一个正常大小的 StarryOS kernel + 一个带完整源码信息的 ELF</span></span></code></pre></div><p>电脑端的静态验证确定可以找到源码文件与源码行，并且可以按源码行设置断点，所以毫无疑问 Host-DWARF 构建的方案是成功的。</p><p>于是开始上版，新的 Host-DWARF kernel 通过 fastboot</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>成功加载到地址：0x140000000 </span></span>
<span class="line"><span>对应 DTB 加载到：0x138000000</span></span></code></pre></div><p>随后在 U-Boot 中执行：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>scsi scan</span></span></code></pre></div><p>确认 UFS 可以正常识别，然后执行：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>booti 0x140000000 - 0x138000000</span></span></code></pre></div><p>最终串口成功出现：Welcome to Starry OS!</p><p>这说明新的带 debug 编译信息的 StarryOS 已经能够在 K3 真板正常启动。同时，本次启动使用的 Debug FSBL 成功输出：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Debug JTAG enabled on MMC1 pins</span></span></code></pre></div><p>说明 JTAG pinmux 已正确建立。</p><p>随后使用之前已验证 OpenOCD 配置，成功连接 K3，并得到了：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>JTAG tap: k3.cpu enabled</span></span>
<span class="line"><span>[k3.x100.0] Examined RISC-V core</span></span>
<span class="line"><span>XLEN=64</span></span>
<span class="line"><span>[k3.x100.0] Examination succeed</span></span>
<span class="line"><span>Listening on port 3333 for gdb connections</span></span></code></pre></div><p>说明 Host-DWARF StarryOS + JTAG + OpenOCD 已经能够同时正常工作。</p><p>随后使用与当前运行 kernel 同一次构建生成的 Host-DWARF ELF 启动 GDB：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gdb-multiarch starryos_host_dwarf_release.elf</span></span></code></pre></div><p>并连接：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>set architecture riscv:rv64</span></span>
<span class="line"><span>target remote localhost:3333</span></span></code></pre></div><p>GDB 成功将真板当前 PC 映射为：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ax_plat::time::current_ticks ()</span></span>
<span class="line"><span>at platforms/ax-plat/src/time.rs:23</span></span></code></pre></div><p>说明已经完成了：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PC → Rust 函数</span></span>
<span class="line"><span>PC → Rust 源文件</span></span>
<span class="line"><span>PC → 源码行号</span></span></code></pre></div><p>随后验证源码断点：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>break platforms/ax-plat/src/time.rs:23</span></span>
<span class="line"><span>continue</span></span></code></pre></div><p>真板成功命中：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Breakpoint 2.9, ax_plat::time::current_ticks ()</span></span>
<span class="line"><span>    at platforms/ax-plat/src/time.rs:23</span></span></code></pre></div><p>进一步执行 next</p><p>成功源码级执行到：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>platforms/axplat-dyn/src/generic_timer.rs:58</span></span></code></pre></div><p>因此验证：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Host-DWARF 构建成功                        ✅</span></span>
<span class="line"><span>Host-DWARF StarryOS 真板启动成功           ✅</span></span>
<span class="line"><span>Debug FSBL / JTAG pinmux                  ✅</span></span>
<span class="line"><span>OpenOCD 真板连接                           ✅</span></span>
<span class="line"><span>GDB remote                                ✅</span></span>
<span class="line"><span>PC → Rust 函数映射                         ✅</span></span>
<span class="line"><span>PC → source file:line 映射                 ✅</span></span>
<span class="line"><span>源码行断点解析                              ✅</span></span>
<span class="line"><span>源码行断点真板命中                          ✅</span></span>
<span class="line"><span>源码级 next                                ✅</span></span></code></pre></div><p>当前完整调试链已经达到：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Rust 源码</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>Host-DWARF ELF</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>GDB</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>OpenOCD</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>J-Link / JTAG</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>K3 RISC-V Debug Module</span></span>
<span class="line"><span>   ↓</span></span>
<span class="line"><span>运行中的 StarryOS</span></span></code></pre></div><p>K3 + StarryOS 的底层源码调试环境已经完成验证，底层硬件调试链已经打通，下一阶段主要工作就是把 ARD 调试器适配已经验证的 GDB/OpenOCD/StarryOS 真板链路上。</p><h2 id="本阶段完成的工作与贡献" tabindex="-1">本阶段完成的工作与贡献 <a class="header-anchor" href="#本阶段完成的工作与贡献" aria-label="Permalink to &quot;本阶段完成的工作与贡献&quot;">​</a></h2><ol><li>修改 FSBL / SPL 的板级初始化，使 MMC1 复用引脚切换为 PRI JTAG，并通过：</li></ol><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Debug JTAG enabled on MMC1 pins</span></span></code></pre></div><p>确认 JTAG pinmux 已建立。</p><ol start="2"><li><p>保证 Full U-Boot 阶段不会重新占用该组 JTAG 引脚，使 JTAG 链路能够在 StarryOS 启动后继续保持有效。</p></li><li><p>通过真板反复验证，确定 K3 当前稳定的 OpenOCD 调试参数，最终固定为 100 kHz、cluster0/core0 的单核调试配置。</p></li><li><p>在启动过程中发现 StarryOS 的 K3 UFS 初始化依赖 U-Boot 前级状态。通过对照实验确认，在启动 StarryOS 前执行：</p></li></ol><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>scsi scan</span></span></code></pre></div><p>可以使后续 StarryOS 正常完成 MPHY、UFS Link、rootfs 挂载并进入 shell，因此该步骤被正式纳入当前 Golden Flow。</p><ol start="5"><li>在基础硬件调试链建立以后，又对 StarryOS 的构建方式进行了扩展。原始 release 版本虽然保留函数符号，但缺少完整 DWARF 源码调试信息，只能做到：PC地址 → 函数名。</li></ol><p>为此新增 Host-DWARF 构建方式，使板子上运行的 kernel 仍保持 release 运行逻辑和正常体积，而调试主机上的 ELF 保留完整源码调试信息。最终形成：正常大小的 StarryOS release kernel + 与其同一次构建生成的 Host-DWARF ELF</p><p>并在 K3 真板上进一步完成：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PC → Rust 函数</span></span>
<span class="line"><span>PC → Rust 源文件</span></span>
<span class="line"><span>PC → 源码行号</span></span>
<span class="line"><span>源码行断点解析</span></span>
<span class="line"><span>源码行断点真板命中</span></span>
<span class="line"><span>源码级 next</span></span></code></pre></div><p>本阶段的核心成果：K3 COM260 + StarryOS 真正建设成了一个可以进行 Rust 源码级硬件调试的目标平台，同时完成了 ARD 真板接入所需的底层环境准备。</p><p>后续 ARD 不再需要重新解决 JTAG、OpenOCD、StarryOS 启动和源码符号问题，而只需要在已经验证成功的链路之上完成 GDB/MI、launch 配置、源码路径、断点管理和调试状态机等适配。</p><p>该阶段工作可以简要概括为：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>K3 + StarryOS 真板运行环境建立</span></span>
<span class="line"><span>→ K3 JTAG 调试能力开启</span></span>
<span class="line"><span>→ OpenOCD/GDB 真板链打通</span></span>
<span class="line"><span>→ StarryOS Host-DWARF 构建能力补充</span></span>
<span class="line"><span>→ Rust 源码级硬件调试闭环完成</span></span>
<span class="line"><span>→ 为 ARD 真板接入建立稳定底层基线</span></span></code></pre></div>`,113)])])}const u=a(t,[["render",i]]);export{g as __pageData,u as default};
