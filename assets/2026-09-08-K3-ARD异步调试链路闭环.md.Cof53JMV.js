import{_ as e,o as n,c as p,a2 as t,j as s}from"./chunks/framework.Ca9F9Y59.js";const i="/k3gdb/assets/k3-ard-async-execution-graph.B0hUk9Fz.png",g=JSON.parse('{"title":"K3 异步调试链路闭环","description":"","frontmatter":{},"headers":[],"relativePath":"2026-09-08-K3-ARD异步调试链路闭环.md","filePath":"2026-09-08-K3-ARD异步调试链路闭环.md"}'),l={name:"2026-09-08-K3-ARD异步调试链路闭环.md"};function d(o,a,c,r,h,u){return n(),p("div",null,[...a[0]||(a[0]=[t(`<h1 id="k3-异步调试链路闭环" tabindex="-1">K3 异步调试链路闭环 <a class="header-anchor" href="#k3-异步调试链路闭环" aria-label="Permalink to &quot;K3 异步调试链路闭环&quot;">​</a></h1><p>前面阶段已经让 StarryOS 在 K3 COM260 上跑了起来，也把 ARD 接到了 K3 上。Rust 源码断点、调用栈读取和 Step Over（单步跳过）都已经验证成功。</p><p>所以这一阶段，继续验证： <strong>ARD 能不能看见 StarryOS 里真正运行的 Rust 异步任务，把执行过程和父子关系展示出来？</strong></p><p>最终，这件事已经在本轮 sleep 2 工作负载中跑通了：白名单能生成，真实运行事件能记录，History 有内容，Snapshot 不再为空，Async Inspector 也能显示两层异步执行关系。验证过程中，还顺手补上了一个功能：<strong>Async only，只把选中的异步函数加入白名单。</strong></p><hr><h2 id="验证环境" tabindex="-1">验证环境 <a class="header-anchor" href="#验证环境" aria-label="Permalink to &quot;验证环境&quot;">​</a></h2><p>这一阶段启动仍然遵循已经确认的条件：先完成一次厂商启动和 RCPU / RPMI 初始化，保持供电，再通过 warm recovery（不断电的热重启恢复流程）进入 Debug Golden Flow，启动 Host-DWARF StarryOS。</p><p>这里的 Host-DWARF，还是之前的做法：<strong>板子运行正常大小的 release BIN，源码调试信息留在电脑端的 ELF 中，两者必须来自同一次构建。</strong></p><p>本轮环境如下：</p><table tabindex="0"><thead><tr><th>项目</th><th>当前配置</th></tr></thead><tbody><tr><td>开发板</td><td>SpacemiT K3 COM260</td></tr><tr><td>操作系统</td><td>StarryOS</td></tr><tr><td>指令集</td><td>RISC-V 64</td></tr><tr><td>调试器</td><td>ARD</td></tr><tr><td>GDB</td><td>gdb-multiarch</td></tr><tr><td>硬件调试通道</td><td>OpenOCD + J-Link + JTAG</td></tr></tbody></table><p>ARD / GDB 使用的 Host-DWARF ELF：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/home/user/ARD_Deploy/deployment_baselines/K3_COM260_StarryOS_DWARF_Debug_Verified/artifacts/starryos/starryos_host_dwarf_release.elf</span></span></code></pre></div><p>ARD 开发仓库与分支保持不变：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>repo: /home/user/async-integration/async-debug</span></span>
<span class="line"><span>branch: async-integration</span></span></code></pre></div><hr><h2 id="找到可观察的异步函数" tabindex="-1">找到可观察的异步函数 <a class="header-anchor" href="#找到可观察的异步函数" aria-label="Permalink to &quot;找到可观察的异步函数&quot;">​</a></h2><p>第一步是在 K3 StarryOS 的 Host-DWARF ELF 上执行 Gen Whitelist。</p><p>这个操作就是<strong>先扫描调试符号，整理出“有哪些函数可以选来观察”的名单。</strong> 它会按照 crate（Rust 的代码包）分类，并区分 async 和 sync 函数。</p><p>一次典型输出是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ARD] wrote grouped whitelist:</span></span>
<span class="line"><span>100 crates (99 user),</span></span>
<span class="line"><span>6296 symbols (77 async, 6219 sync)</span></span></code></pre></div><p>不同调试会话里，同步符号总量有一定波动，但这轮识别到的异步候选数稳定为：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>77 async symbols</span></span></code></pre></div><p>生成的两个文件分别是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tgoskits/temp/poll_functions_grouped.json</span></span>
<span class="line"><span>tgoskits/temp/poll_functions.txt</span></span></code></pre></div><p>它们的分工不同：</p><table tabindex="0"><thead><tr><th>文件</th><th>作用</th></tr></thead><tbody><tr><td>poll_functions_grouped.json</td><td>保存 crate、symbol、source、kind 等分类结果，供界面选择</td></tr><tr><td>poll_functions.txt</td><td>保存最终选中的函数，是运行时真正加载的白名单</td></tr></tbody></table><p>同时需要注意：<strong>Gen Whitelist 完成后，默认的运行时白名单仍然为空，还需要进一步 Apply。</strong> 找到候选函数，并不等于已经开始观察所有函数。</p><hr><h2 id="选择可反复触发的验证对象" tabindex="-1">选择可反复触发的验证对象 <a class="header-anchor" href="#选择可反复触发的验证对象" aria-label="Permalink to &quot;选择可反复触发的验证对象&quot;">​</a></h2><p>有了 77 个 async 候选以后，先选了 ax_task 作为第一轮实验对象。</p><p>它的分类结果是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ax_task:</span></span>
<span class="line"><span>total = 202</span></span>
<span class="line"><span>async = 2</span></span>
<span class="line"><span>sync = 200</span></span></code></pre></div><p>其中两个 async symbol 是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ax_task::future::time::{impl#1}::poll</span></span>
<span class="line"><span>ax_task::future::time::sleep_until::{async_fn#0}</span></span></code></pre></div><p>对应源码位于：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>os/arceos/modules/axtask/src/future/time.rs</span></span></code></pre></div><p>继续沿着 StarryOS 的路径往下看，确认用户态睡眠请求最终会进入异步 sleep 路径。涉及的关键调用包括：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sys_nanosleep</span></span>
<span class="line"><span>sleep_impl</span></span>
<span class="line"><span>block_on(interruptible(sleep(dur)))</span></span>
<span class="line"><span>ax_task::future::time::sleep</span></span>
<span class="line"><span>sleep_until(...).await</span></span>
<span class="line"><span>TimerFuture::poll</span></span></code></pre></div><p>所以第一轮真板验证选用的工作负载很简单，在 StarryOS shell 中执行：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sleep 2</span></span></code></pre></div><p>这样既容易重复，也能让我们选中的异步路径真正运行起来。</p><hr><h2 id="history-和-snapshot" tabindex="-1">History 和 Snapshot <a class="header-anchor" href="#history-和-snapshot" aria-label="Permalink to &quot;History 和 Snapshot&quot;">​</a></h2><p>第一轮只安装一个 Observer，也就是一个运行时观察点：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ax_task::future::time::sleep_until::{async_fn#0}</span></span></code></pre></div><p>加载结果是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ARD] whitelist loaded:</span></span>
<span class="line"><span>exact=1</span></span>
<span class="line"><span>prefix=0</span></span>
<span class="line"><span>observers=1</span></span></code></pre></div><p>这里的 exact=1 表示精确匹配一个符号，prefix=0 表示没有使用前缀匹配。</p><p>随后把同一个函数设为 Trace Root，也就是当前观察视图的根，再到 StarryOS shell 执行 sleep 2。</p><p>于是History 中出现了运行记录：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>func = ax_task::future::time::sleep_until::{async_fn#0}</span></span>
<span class="line"><span>origin = runtime-event</span></span>
<span class="line"><span>semantic_kind = future_poll</span></span>
<span class="line"><span>admission_action = ALLOW</span></span>
<span class="line"><span>admission_reason = whitelist_runtime_execution_hit</span></span></code></pre></div><p>同时记录到了实际的函数进入和退出事件：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>call_enter</span></span>
<span class="line"><span>call_exit</span></span></code></pre></div><p>这一步证明：<strong>这个函数在 K3 板上执行过的记录。</strong> 精确 Observer、RuntimeEvent（运行事件）和 History（执行历史）验证通过。</p><p>接下来查看 Snapshot ，它负责展示当前暂停时的快照。</p><p>在这轮真实运行事件产生后，Snapshot 恢复了非空的异步路径：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>empty = false</span></span></code></pre></div><p>识别到的 Future 是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ax_task::future::time::sleep_until::{async_fn#0}</span></span></code></pre></div><p>从 DWARF 调试信息中恢复出的类型是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ax_task::future::time::sleep_until::{async_fn_env#0}</span></span></code></pre></div><p>同时获得了：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>future_type_source = dwarf</span></span>
<span class="line"><span>poll.status = ok</span></span>
<span class="line"><span>poll.source = dwarf</span></span>
<span class="line"><span>active = true</span></span></code></pre></div><p>源码也成功定位到：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>os/arceos/modules/axtask/src/future/time.rs:129</span></span></code></pre></div><p>这说明，在当前验证对象上，ARD 已经能把真板运行中的 Future、DWARF 类型、poll 状态和源码位置对应起来，并恢复当前活跃的异步路径。</p><hr><h2 id="增加观察点" tabindex="-1">增加观察点 <a class="header-anchor" href="#增加观察点" aria-label="Permalink to &quot;增加观察点&quot;">​</a></h2><p>一个 Future 能看见以后，我们继续把第二个异步函数加进来。</p><p>第二轮使用两个精确 Observer：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>0 ax_task::future::time::sleep_until::{async_fn#0}</span></span>
<span class="line"><span>1 ax_task::future::time::{impl#1}::poll</span></span></code></pre></div><p>加载结果：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>exact = 2</span></span>
<span class="line"><span>prefix = 0</span></span>
<span class="line"><span>observers = 2</span></span></code></pre></div><p>再次执行 sleep 2 后，History 得到了：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>nodes = 2</span></span>
<span class="line"><span>edges = 1</span></span>
<span class="line"><span>roots = 1</span></span>
<span class="line"><span>events = 5</span></span></code></pre></div><p>简单说，就是有两个节点、一条连接关系、一个根节点，以及五个运行事件。</p><p>Child 节点还明确记录了父节点及关系来源：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>parent_func = ax_task::future::time::sleep_until::{async_fn#0}</span></span>
<span class="line"><span>parent_source = recent_root_thread</span></span>
<span class="line"><span>edge_status = accepted</span></span>
<span class="line"><span>event_gap = 1</span></span></code></pre></div><p>恢复出来的两层关系如下：</p>`,79),s("table",{tabindex:"0"},[s("thead",null,[s("tr",null,[s("th",null,"层级"),s("th",null,"函数")])]),s("tbody",null,[s("tr",null,[s("td",null,"父节点"),s("td",{"async_fn#0":""},"ax_task::future::time::sleep_until::")]),s("tr",null,[s("td",null,"子节点"),s("td",null,"ax_task::future::time::{impl#1}::poll")])])],-1),t('<p>说明 <strong>ARD 可以在本轮真板事件中建立两层 async 父子关系。</strong> 关系来源保留在 parent_source 等字段中，便于回看建立过程。</p><hr><h2 id="inspector界面" tabindex="-1">Inspector界面 <a class="header-anchor" href="#inspector界面" aria-label="Permalink to &quot;Inspector界面&quot;">​</a></h2><p>后端数据正确后，还要确认这些结果在 Async Inspector 中展示。</p><p>继续以 sleep_until::{async_fn#0} 作为 Trace Root ，Observer Tree 从 History 中投影出了相同的两层结构，Execution Graph UI 也显示了对应的父子节点。</p><p><img src="'+i+`" alt="K3 真板 ARD Async Execution Graph"></p><p>图中可以看到 sleep_until::{async_fn#0} 与 ax_task::future::time::{impl#1}::poll 的两层异步执行关系。</p><p>图中这几个组件各有分工：</p><table tabindex="0"><thead><tr><th>组件</th><th>本轮验证中的作用</th></tr></thead><tbody><tr><td>RuntimeEvent</td><td>记录真板上实际发生的运行事件</td></tr><tr><td>History</td><td>保存事件及节点、关系等执行历史</td></tr><tr><td>Snapshot</td><td>查看当前暂停时的异步状态</td></tr><tr><td>Observer Tree</td><td>按选定 Trace Root 展示 History 中的关系</td></tr><tr><td>RuntimeEventGraph</td><td>表达运行事件形成的节点和连接关系</td></tr><tr><td>Execution Graph UI</td><td>在 Async Inspector 中把关系展示出来</td></tr></tbody></table><p>本轮验证把“真板实际执行—后端记录—关系恢复—界面展示”接起来了。</p><hr><h2 id="只把-async-函数加入白名单" tabindex="-1">只把 async 函数加入白名单 <a class="header-anchor" href="#只把-async-函数加入白名单" aria-label="Permalink to &quot;只把 async 函数加入白名单&quot;">​</a></h2><p>原有 Apply Whitelist 按 crate 选择。以使用的 ax_task 为例：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>202 total</span></span>
<span class="line"><span>2 async</span></span>
<span class="line"><span>200 sync</span></span></code></pre></div><p>里面有 2 个异步函数和 200 个同步函数，这些都会一起进入运行时白名单。</p><p>但如果只想观察异步函数。把其他函数也装成 Observer，会增加观察点数量、运行开销和无关信息。</p><p>于是补上一个小功能：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ ] Async only</span></span></code></pre></div><p><strong>只把已经选中 crate 里的 async 函数加入白名单。</strong></p><p>这个选项默认关闭，关闭时保持原来的 Apply 行为；开启后，只保留已有 grouped whitelist 中 kind == &quot;async&quot; 的符号。</p><table tabindex="0"><thead><tr><th>选项状态</th><th>Apply Whitelist 的结果</th></tr></thead><tbody><tr><td>Async only 关闭</td><td>加入所选 crate 的全部 symbols</td></tr><tr><td>Async only 开启</td><td>只加入所选 crate 中 kind == &quot;async&quot; 的 symbols</td></tr></tbody></table><p>它直接复用：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>poll_functions_grouped.json</span></span></code></pre></div><p>不会重新扫描 ELF，也不会再额外生成一份候选函数名单。</p><p>实现以后，在 Async Inspector 中选中 ax_task，打开 Async only，再点击 Apply Whitelist。</p><p>输出变成：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ARD] whitelist updated:</span></span>
<span class="line"><span>1 crates enabled,</span></span>
<span class="line"><span>2 symbols,</span></span>
<span class="line"><span>observers=2,</span></span>
<span class="line"><span>async_only=true</span></span></code></pre></div><p>随后加载结果为：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ARD] whitelist loaded:</span></span>
<span class="line"><span>exact=2</span></span>
<span class="line"><span>prefix=0</span></span>
<span class="line"><span>observers=2</span></span></code></pre></div><p>实际生成的文件：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/home/user/ARD_Deploy/tgoskits/temp/poll_functions.txt</span></span></code></pre></div><p>确实只包含两条 async symbol：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>0 ax_task::future::time::{impl#1}::poll</span></span>
<span class="line"><span>1 ax_task::future::time::sleep_until::{async_fn#0}</span></span></code></pre></div><p>没有把同一个 crate 里的 200 个同步符号一起带进来。</p><p>接着再次执行 sleep 2，Execution Graph 仍然能恢复。</p><p>这一步同时验证了两件事：</p><ul><li>Async only 确实筛出了我们想要的两个异步函数；</li><li>筛选后，原本已经跑通的 RuntimeEvent、History、Observer Tree 和 Execution Graph 链路仍然正常。</li></ul><hr><h2 id="本阶段完成的工作与贡献" tabindex="-1">本阶段完成的工作与贡献 <a class="header-anchor" href="#本阶段完成的工作与贡献" aria-label="Permalink to &quot;本阶段完成的工作与贡献&quot;">​</a></h2><p>这一阶段主要完成了四件事。</p><ol><li><p><strong>把已有异步观察能力放到了真实 K3 环境中验证。</strong> 从 Host-DWARF ELF 生成白名单，再用 StarryOS 的 sleep 2 触发实际异步路径，确认记录来自真板执行，而不只是静态符号。</p></li><li><p><strong>把单 Future 的观察推进到了两层关系和界面展示。</strong> History 记录到节点和事件，Snapshot 恢复出 Future 类型、poll 状态与源码位置，Observer Tree 和 Execution Graph 显示出同一组父子关系。</p></li><li><p><strong>补上 Async only 白名单筛选。</strong> 复用现有 grouped whitelist 的 kind 字段，让用户在选择 crate 后只加载 async 函数，保留默认关闭时的兼容行为。</p></li><li><p><strong>完成测试、真板回归和提交。</strong> Async only 不只是写好了界面选项，而是已经验证到运行时白名单和最终 Execution Graph，并完成代码推送。</p></li></ol><p>其中，前两项主要是在 K3 上验证 ARD 已有能力；Async only 是本阶段新增的功能。两者一起构成了这次阶段收尾的成果。</p><hr><h2 id="当前阶段结果" tabindex="-1">当前阶段结果 <a class="header-anchor" href="#当前阶段结果" aria-label="Permalink to &quot;当前阶段结果&quot;">​</a></h2><p>验证结果汇总如下。这里的 PASS 对应本轮已记录的环境和测试范围：</p><table tabindex="0"><thead><tr><th>能力</th><th>状态</th></tr></thead><tbody><tr><td>K3 ARD 基础连接</td><td>✅ PASS</td></tr><tr><td>Host-DWARF 源码调试</td><td>✅ PASS</td></tr><tr><td>Gen Whitelist</td><td>✅ PASS</td></tr><tr><td>Async symbol 识别</td><td>✅ PASS</td></tr><tr><td>精确 Runtime Observer</td><td>✅ PASS</td></tr><tr><td>RuntimeEvent</td><td>✅ PASS</td></tr><tr><td>History</td><td>✅ PASS</td></tr><tr><td>Snapshot 非空异步路径</td><td>✅ PASS</td></tr><tr><td>DWARF Future 类型恢复</td><td>✅ PASS</td></tr><tr><td>Observer Tree</td><td>✅ PASS</td></tr><tr><td>本轮两层 async 父子关系</td><td>✅ PASS</td></tr><tr><td>RuntimeEventGraph</td><td>✅ PASS</td></tr><tr><td>Execution Graph UI</td><td>✅ PASS</td></tr><tr><td>Async only UI</td><td>✅ PASS</td></tr><tr><td>Async only 运行时筛选</td><td>✅ PASS</td></tr><tr><td>Async only K3 端到端回归</td><td>✅ PASS</td></tr></tbody></table><p>对应的阶段结果标记保留如下，便于与原始验证记录对照：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>K3_STARRYOS_WHITELIST_GENERATION_VERIFIED = PASS</span></span>
<span class="line"><span>K3_STARRYOS_WHITELIST_OBSERVERS_VERIFIED = PASS</span></span>
<span class="line"><span>K3_STARRYOS_RUNTIME_EVENT_VERIFIED = PASS</span></span>
<span class="line"><span>K3_STARRYOS_HISTORY_VERIFIED = PASS</span></span>
<span class="line"><span>K3_STARRYOS_SNAPSHOT_VERIFIED = PASS</span></span>
<span class="line"><span>K3_STARRYOS_ASYNC_PARENT_CHILD_VERIFIED = PASS</span></span>
<span class="line"><span>K3_STARRYOS_RUNTIME_EVENT_GRAPH_VERIFIED = PASS</span></span>
<span class="line"><span>K3_STARRYOS_OBSERVER_TREE_VERIFIED = PASS</span></span>
<span class="line"><span>K3_STARRYOS_ASYNC_EXECUTION_GRAPH_UI_VERIFIED = PASS</span></span>
<span class="line"><span>K3_STARRYOS_ASYNC_RUNTIME_END_TO_END_VERIFIED = PASS</span></span>
<span class="line"><span>K3_ASYNC_ONLY_WHITELIST_UI_VERIFIED = PASS</span></span>
<span class="line"><span>K3_ASYNC_ONLY_FILTER_VERIFIED = PASS</span></span>
<span class="line"><span>K3_ASYNC_ONLY_RUNTIME_REGRESSION = PASS</span></span>
<span class="line"><span>K3_ASYNC_ONLY_WHITELIST_END_TO_END_VERIFIED = PASS</span></span>
<span class="line"><span>STAGE4_K3_ARD_ASYNC_RUNTIME = PASS</span></span></code></pre></div><p>因此 <strong>ARD 已经能够在 K3 真板上观察本轮 StarryOS 异步路径，记录真实事件，恢复 Future 状态与两层父子关系，并在 Async Inspector 中展示执行图。</strong></p><hr><h2 id="下一阶段" tabindex="-1">下一阶段 <a class="header-anchor" href="#下一阶段" aria-label="Permalink to &quot;下一阶段&quot;">​</a></h2><p>下一阶段是另一个问题： <strong>ARD 能不能进一步在 K3 + StarryOS 上识别操作系统的 kernel/user 状态转换？</strong></p><p>也就是用户程序进入内核、再返回用户态时，OSStateMachine（操作系统状态机）能否跟上真实执行过程。</p>`,53)])])}const _=e(l,[["render",d]]);export{g as __pageData,_ as default};
